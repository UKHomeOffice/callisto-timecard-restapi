apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-volume-claim
  namespace: callisto-{{ .Values.environment }}
spec:
  storageClassName: io2-encrypted-eu-west-2
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 4Gi