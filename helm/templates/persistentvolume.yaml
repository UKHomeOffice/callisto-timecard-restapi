apiVersion: v1
kind: PersistentVolume
metadata:
  name: callisto-keystore-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/tmp/keystore-volume"